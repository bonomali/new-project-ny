apiVersion: v1
kind: Service
metadata:
  name: npny-mongo-service
  labels:
    app: hello-world
spec:
  ports:
    - port: 27017
  selector:
    app: hello-world
    tier: mongo
  clusterIP: None
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: npny-mongo-pv-claim
  labels:
    app: hello-world
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: npny-mongo
  labels:
    app: hello-world
spec:
  selector:
    matchLabels:
      app: hello-world
      tier: mongo
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: hello-world
        tier: mongo
    spec:
      containers:
        - image: mongo:4.2.6
          name: npny-mongo
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: root
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-pass
                  key: password
          ports:
            - containerPort: 27017
              name: mongo
          volumeMounts:
            - name: mongo-persistent-storage
              mountPath: /data/db
      volumes:
        - name: mongo-persistent-storage
          persistentVolumeClaim:
            claimName: npny-mongo-pv-claim
